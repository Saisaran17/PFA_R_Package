# sum of n numbers
#
# This is an example function named 'sum_of_numbers'
# This returns the sum of n numbers
#


sum_of_numbers <- function(num) {

    sum = (num * (num + 1)) / 2;
    return(sum)

}
